<Window x:Class="MindMapAI.TagManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MindMapAI.ViewModels"
        Title="Управление тегами" 
        Width="307" Height="475"
        WindowStartupLocation="Manual"
        Top="131" Left="504"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <SolidColorBrush x:Key="BgPrimary" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="BgSecondary" Color="#242434"/>
        <SolidColorBrush x:Key="BgCard" Color="#2A2A3E"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#CDD6F4"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A6ADC8"/>
        <SolidColorBrush x:Key="BorderColor" Color="#313244"/>
        <SolidColorBrush x:Key="AccentColor" Color="#89B4FA"/>
        <SolidColorBrush x:Key="DangerColor" Color="#F38BA8"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TagItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="{StaticResource BgSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Margin="{TemplateBinding Margin}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" 
                                           Text="{Binding Name}" 
                                           Foreground="{StaticResource TextPrimary}"
                                           VerticalAlignment="Center"
                                           FontSize="13"/>
                                <Button x:Name="EditTagBtn"
                                        Grid.Column="1"
                                        Width="20" Height="20"
                                        Margin="0,0,8,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Visibility="Collapsed"
                                        Click="EditTag_Click"
                                        AutomationProperties.Name="Редактировать тег"
                                        AutomationProperties.HelpText="Изменить название тега">
                                    <Image Source="/Assets/Images/pencil-icon.png" Width="16" Height="16"/>
                                </Button>
                                <Button x:Name="DeleteTagBtn"
                                        Grid.Column="2"
                                        Width="20" Height="20"
                                        Background="Transparent"
                                        BorderThickness="0"         
                                        Cursor="Hand"   
                                        Visibility="Collapsed"
                                        Click="DeleteTag_Click"
                                        AutomationProperties.Name="Удалить тег"
                                        AutomationProperties.HelpText="Удалить тег из системы">
                                    <Image Source="/Assets/Images/trash.png" Width="16" Height="16"/>
                                </Button>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="EditTagBtn" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="DeleteTagBtn" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6C9FEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgPrimary}"
        CornerRadius="12"
        BorderBrush="{StaticResource BorderColor}"
        BorderThickness="1">
        <Grid Margin="15,25,18,15">
            <!-- Кнопка закрытия (крестик) -->
            <Button x:Name="CloseButton"
        HorizontalAlignment="Right"
        VerticalAlignment="Top"
        Width="30" 
        Height="30"
        Margin="0,-5,-5,0"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Click="CloseButton_Click"
        AutomationProperties.Name="Закрыть"
        AutomationProperties.HelpText="Закрыть окно управления тегами">
                <Image Source="/Assets/Images/close-icon.png" 
           Width="30" 
           Height="30"/>
            </Button>

            <Border Background="{StaticResource BgPrimary}"
            CornerRadius="12"
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1" Margin="0,16,0,0">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <TextBlock Grid.Row="0" 
                       Text="Теги заметки"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"
                       Margin="89,5,-89,10"/>

                    <!-- Список тегов -->
                    <Border Grid.Row="2"
                    Background="{StaticResource BgSecondary}"
                    CornerRadius="8"
                    Padding="8"
                    MinHeight="300">
                        <ListBox x:Name="TagsListBox"
                         ItemsSource="{Binding Tags}"
                         ItemContainerStyle="{StaticResource TagItemStyle}"
                         Background="Transparent"
                         BorderThickness="0"
                         AutomationProperties.Name="Список тегов"
                         AutomationProperties.HelpText="Список тегов, привязанных к заметке"/>
                    </Border>

                    <!-- Панель для нового тега -->
                    <StackPanel Grid.Row="3" Margin="0,15,0,0">
                        <Button x:Name="NewTagButton"
                        Content="+ Новый тег"
                        Style="{StaticResource ModernButton}"
                        HorizontalAlignment="Center"
                        Width="120"
                        Click="NewTagButton_Click"
                        Visibility="Visible"
                        AutomationProperties.Name="Добавить тег"
                        AutomationProperties.HelpText="Создать и привязать новый тег к заметке"/>

                        <TextBox x:Name="NewTagBox"
                         Style="{StaticResource ModernTextBox}"
                         Tag="Введите тег и нажмите Enter..."
                         Margin="0,8,0,0"
                         KeyDown="NewTagBox_KeyDown"
                         Visibility="Collapsed"
                         AutomationProperties.Name="Новый тег"
                         AutomationProperties.HelpText="Введите название тега и нажмите Enter"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
   </Border>
</Window>